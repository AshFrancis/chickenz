[package]
name = "chickenz-chunk-guest"
version = "0.1.0"
edition = "2021"

[dependencies]
chickenz-core = { path = "../core" }
risc0-zkvm = { version = "3.0", default-features = false, features = ["std"] }
bytemuck = { version = "1", features = ["derive"] }
sha2 = "0.10"
